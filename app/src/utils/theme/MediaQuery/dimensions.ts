import theme from '@/styles/theme';

export const desktopDimensions = { maxWidth: theme.breakpoints.desktop.maxWidth };
export const tabletDimensions = { maxWidth: theme.breakpoints.tablet.maxWidth };
export const mobileDimensions = { maxWidth: theme.breakpoints.mobile.maxWidth };
